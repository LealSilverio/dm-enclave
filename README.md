# DM-'s Enclave

dev build:

```shell
pnpm i
```

for dev env w/ nodemon:

```shell
pnpm dev
```

for prod startup, there's now a required build step:

```shell
pnpm build
```

```shell
pnpm start
```
